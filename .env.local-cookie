VITE_BASE_URL = '/'
VITE_ROUTER_URL = ''
VITE_API = '/front'
VITE_HOST = 'http://localhost:8900'
# 认证模式：jwt（token 前端存储）/ cookie（HttpOnly Cookie 由浏览器管理）
# 测试环境用 cookie 模式本地验证 HttpOnly，需后端同时以 AUTH_MODE=cookie 启动
VITE_AUTH_MODE = 'cookie'
